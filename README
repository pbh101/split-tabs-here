To add to Chrome from source, load Chrome's extensions page, pop into Developer Mode, and select the 'Load unpacked extension...' option, and point it to the directory.

The icon is from the "merge windows" Google Chrome extension sample. 
